<!-- <h4>Если бы вы были гражданином нашего мира и могли бы вместе с нами голосовать за погоду по утрам, какую погоду вы бы предпочитали большую часть своего времени?</h4>
 Температура
    Облачно,ясно
    Дождливо сухо
    Жить все время в лете, зиме, осене, весне, либо чтобы была обычная смена сезонов</h4> 

погода мечты по горизонталям-->
<script>
    const surveyForm = document.getElementById("surveyForm");
    const showAnswersButton = document.getElementById("showAnswersButton");
    const answersContainer = document.getElementById("answersContainer");

    surveyForm.addEventListener("submit", function (event) {
        event.preventDefault();

        const name = document.getElementById("nameImput").value;
        const temperature = document.getElementById("temperatureSelect").value;
        const clouds = document.querySelector(
            'input[name="clouds"]:checked'
        ).value;
        const humidity = document.querySelector(
            'input[name="humidity"]:checked'
        ).value;
        const season = document.getElementById("seasonSelect").value;

        const surveyFormData = {
            name: name,
            temperature: temperature,
            clouds: clouds,
            humidity: humidity,
            season: season,
        };
        // showAnswersButton.dataset.surveyFormData = JSON.stringify(
        //     Object.values(surveyFormData)
        // );
    });
    // // showAnswersButton.addEventListener("click", function () {
    // //     const surveyFormData = JSON.parse(
    // //         showAnswersButton.dataset.surveyFormData || "{}"
    // //     );
    // //     answersContainer.textContent = JSON.stringify(surveyFormData, null, 2);
    // });
</script>

<!-- <script>
    const nextPage = document.getElementById("nextPage");
    const hiddenGame = document.getElementById("hiddenGame");
    const hideAfter = document.getElementById("hideAfter");
    // nextPage.addEventListener("click", () => {
    //     hiddenGame.classList.toggle("hide");
    //     hideAfter.classList.toggle("hide");
    // });
</script> -->
<style>
    .hide {
        display: none;
    }
    main {
        position: relative;
    }

    .form-container {
        display: flex;
        align-items: center;
        flex-direction: column;
    }

    .question {
        font-size: 1.3rem;
        padding: 10px 15px;
        width: 600px;
        text-align: center;
        margin-bottom: 30px;
    }

    .span-question {
        color: rgb(251, 221, 157);
        background-color: #3488a8;
        padding: 1px 3px;
        border-radius: 5px;
    }

    .p-vertical {
        writing-mode: vertical-rl;
        text-orientation: upright;
        position: absolute;
        font-size: 4.2rem;
        margin: 0;
        background: linear-gradient(
            rgb(119, 43, 134),
            #86b1c1 40%,
            #c9f0ff 80%
        );
        width: 116px;
        line-height: 116px;
        color: white;
        font-weight: bold;
        padding-top: 10px;
        padding-bottom: 10px;
        bottom: 25px;
        opacity: 0.8;
    }

    .p-vertical-weather {
        top: -20px;
        left: -40px;
    }
    .p-vertical-dream {
        top: -20px;
        right: -40px;
    }
    form {
        display: flex;
        align-items: flex-start;
        flex-direction: column;
        row-gap: 20px;
    }

    label {
        width: 400px;
    }
    option {
    }
    .container {
        display: flex;
        align-items: flex-start;
        flex-direction: column;
        background-color: rgb(251, 221, 157);
        padding: 10px 15px;
        border-radius: 10px 10px 10px 0;
        border-bottom: 5px solid #c9f0ff;
        border-left: 5px solid #3488a8;
    }
    h3 {
        font-size: 1.3rem;
        margin: 0;
        width: 240px;
        color: black;
        border-radius: 8px;
    }

    .p-label {
        margin: 5px 0 10px;
    }

    input,
    select {
        border-radius: 6px;
    }
    .submit {
        background-color: rgb(119, 43, 134);
        border-radius: 18px;
        padding: 10px 16px;
        font-size: 1rem;
        align-self: center;
        color: white;
        font-weight: bold;
        border-color: #539ebc;
        text-decoration: none;
    }
    .submit:hover {
        cursor: pointer;
    }
    button {
        align-self: center;
        background-color: #3488a8;
    }
</style>

<main>
    <!-- <div id="hiddenGame" class="hide">
        <p class="p-vertical p-vertical-weather">ПОГОДАМЕ</p>
        <p class="p-vertical p-vertical-dream">МЕЧТЫПО</p>
       
    </div> -->

    <div id="hideAfter">
        <p class="p-vertical p-vertical-weather">ПОГОДАМЕЧТЫ</p>
        <p class="p-vertical p-vertical-dream">МЕЧТЫПОГОДА</p>
        <div class="form-container">
            <h2 class="question">
                Если бы ты был гражданином нашего мира и мог бы вместе с нами
                голосовать за погоду по утрам, <span class="span-question"
                    >какую погоду</span
                > ты бы предпочитал <span class="span-question"
                    >большую часть времени?</span
                >
            </h2>
            <form id="surveyForm">
                <div class="name-container container">
                    <label for="nameImput"><h3>Твое имя:</h3> <br /></label>
                    <input type="text" name="name" id="nameImput" />
                </div>
                <div class="temperature-container container">
                    <label for="temperatureSelect"
                        ><h3>Температура</h3>
                        <p class="p-label">
                            Какая температура для тебя наиболее комфортна?
                        </p>
                    </label>
                    <select id="temperatureSelect">
                        <option value="от -10 до 0">от -10 до 0</option>
                        <option value="от 0 до +15">от 0 до +15</option>
                        <option value="от +10 до +20">от +10 до +20</option>
                        <option value="от +15 до +25">от +15 до +25</option>
                        <option value="от +20 до +25">от +20 до +25</option>
                        <option value="от +25 до +35">от +25 до +35</option>
                        <option value="больше +35">больше +35</option>
                    </select>
                </div>
                <div class="clouds-container container">
                    <label for="cloudsRadio"
                        ><h3>Количество солнца</h3>
                        <p class="p-label">
                            Тебе бы хотелось, чтобы большую часть времени было:
                        </p>
                    </label>
                    <div class="wrapRadio">
                        <input
                            type="radio"
                            id="cloudsYes"
                            name="clouds"
                            value="Облачно"
                            required
                        /> Облачно
                        <input
                            type="radio"
                            id="cloudsNo"
                            name="clouds"
                            value="Ясно"
                        /> Ясно
                    </div>
                </div>
                <div class="humidity-container container">
                    <label for="humidityRadio"
                        ><h3>Влажность</h3>
                        <p class="p-label">
                            Тебе бы хотелось, чтобы большую часть времени было:
                        </p>
                    </label>
                    <div class="wrapRadio">
                        <input
                            type="radio"
                            id="humidityYes"
                            name="humidity"
                            value="Дождливо/Влажно"
                        /> Дождливо/Влажно
                        <input
                            type="radio"
                            id="humidityNo"
                            name="humidity"
                            value="Сухо"
                        /> Сухо
                    </div>
                </div>
                <div class="season-container container">
                    <label for="seasonSelect"
                        ><h3>Cезон</h3>
                        <p class="p-label">
                            В каком сезоне тебе бы хотелось проживать большую
                            часть времени?
                        </p>
                    </label>
                    <select id="seasonSelect">
                        <option value="Лето">Лето</option>
                        <option value="Осень">Осень</option>
                        <option value="Весна">Весна</option>
                        <option value="Зима">Зима</option>
                        <option value="С обычной сменой сезонов"
                            >С обычной сменой сезонов</option
                        >
                    </select>
                </div>

                <button type="submit" class="submit">Тык для сохранения</button>
                <!-- <button id="showAnswersButton">Покажи мне мои ответы</button> -->
                <a class="submit" href="./gameForWeather">След страница</a>
            </form>

            <!-- <div id="answersContainer"></div> -->
        </div>
    </div>
</main>
